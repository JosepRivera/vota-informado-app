package com.rivera.votainformado.ui.theme

import androidx.compose.ui.graphics.Color

// Azules institucionales - Transmiten confianza y profesionalismo
val InstitutionalBlue = Color(0xFF1565C0) // Azul principal institucional
val InstitutionalBlueLight = Color(0xFF42A5F5) // Azul claro para hover/activo
val InstitutionalBlueDark = Color(0xFF0D47A1) // Azul oscuro para encabezados

// Verdes cívicos - Representan democracia y participación
val CivicGreen = Color(0xFF2E7D32) // Verde cívico principal
val CivicGreenLight = Color(0xFF66BB6A) // Verde claro
val CivicGreenDark = Color(0xFF1B5E20) // Verde oscuro

// Neutrales modernos
val NeutralWhite = Color(0xFFFFFFFF)
val NeutralLight = Color(0xFFF8F9FA) // Fondo muy suave
val NeutralGray = Color(0xFFE9ECEF) // Divisores suaves
val NeutralMedium = Color(0xFF6C757D) // Texto secundario
val NeutralDark = Color(0xFF212529) // Texto principal

// Acentos funcionales
val AccentAmber = Color(0xFFF57C00) // Naranja para llamados a la acción
val AccentTeal = Color(0xFF00897B) // Verde azulado para información
val AccentIndigo = Color(0xFF3949AB) // Índigo para datos destacados

// Estados
val SuccessTeal = Color(0xFF00695C) // Éxito
val WarningAmber = Color(0xFFEF6C00) // Advertencia
val ErrorRed = Color(0xFFD32F2F) // Solo para errores reales
val InfoBlue = Color(0xFF1976D2) // Información

// Modo oscuro
val DarkBg = Color(0xFF0A1929) // Fondo oscuro azulado
val DarkSurf = Color(0xFF1A2332) // Superficie oscura
val DarkSurfVar = Color(0xFF243447) // Variante de superficie

/**
// ====================================
// VERSIÓN 2: PALETA PERUANA (Rojo y Blanco)
// ====================================

// Rojos principales - Inspirados en la bandera peruana
val PeruRed = Color(0xFFD91023) // Rojo bandera oficial del Perú
val PeruRedLight = Color(0xFFFF4459) // Rojo claro para elementos interactivos
val PeruRedDark = Color(0xFFA00818) // Rojo oscuro para énfasis

// Blancos y grises neutros
val PeruWhite = Color(0xFFFFFFFF)
val LightGray = Color(0xFFF5F5F5) // Fondo suave
val MediumGray = Color(0xFFE0E0E0) // Bordes y divisores
val DarkGray = Color(0xFF424242) // Texto secundario
val CharcoalGray = Color(0xFF212121) // Texto principal

// Colores complementarios para evitar que todo parezca error
val AccentBlue = Color(0xFF1976D2) // Azul para acciones primarias positivas
val AccentBlueDark = Color(0xFF0D47A1) // Azul oscuro
val AccentBlueLight = Color(0xFF42A5F5) // Azul claro

val SuccessGreen = Color(0xFF2E7D32) // Verde para éxito/confirmación
val WarningOrange = Color(0xFFEF6C00) // Naranja para advertencias
val InfoCyan = Color(0xFF0097A7) // Cyan para información

// Fondos para modo oscuro
val DarkBackground = Color(0xFF121212)
val DarkSurface = Color(0xFF1E1E1E)
val DarkSurfaceVariant = Color(0xFF2C2C2C) **/